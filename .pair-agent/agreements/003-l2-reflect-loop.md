# 合意ドキュメント 003 — L2（PDCA / Reflect ループ）

- **sprint**: 003-l2-reflect-loop
- **status**: active
- **開始**: 2026-07-03
- **前提**: L0（理想化）・L1（ツールループ ＝ D）完成

---

## ゴール

L2 = **PDCA を明示的に乗せる層**。D は L1。L2 が足すのは **Reflect（C＋A）**。
「与えられたゴールに向かって自立実行」の最小形。

## 構造（確定）

- **再帰同一構造**: どの層も「**内側ループを規定回数回す → Reflect で判断 → 続行/停止**」。L2 が L1 を、（将来）L3 が L2 を、**同じ形**で管理する。層が増えても構造は1つ。
- **再帰の底 = 人間**（最上位の停止は人間）。各層は安全のため上限を持つが、**上限は自層ハードコードでなく呼び出し側が規定**（`l2_chat.py`／テスト／将来の L3）。
- L1 が無状態なので、N 回で切って Reflect を挟み、未達なら messages 再投入で再開しても**仕事が失われない**。

## L2 の定義

- `run(goal, tools, max_rounds, l1_max) → (messages, passed)`:
  - loop（`max_rounds` 上限・呼び出し側規定）:
    1. **D**: L1 を `l1_max` 回まで回す
    2. **C＋A（Reflect）**: 「goal は達成されたか？ 未達なら何が足りないか」を **構造化出力 `{passed: bool, reason: str, next: str}`** で1回問う
    3. `passed` → 終了 ／ 未達 → `next` を messages に再投入して継続（＝**そこで計画が生まれる**）
- **Plan は暗黙**（Reflect の `next` 再投入）。明示 Plan は後段。
- Reflect は **L0 の構造化出力**（`format` にスキーマ）で合否を確実に取る。
- **無状態**: 状態は messages（上位が持つ）。中断・再開は L1 同様。

## テストケース（難しいお題で Reflect を鍛える）

| # | お題 | 試す点 |
|---|---|---|
| T1 | `mu/` の全 .py を読み各役割1行を `overview.md` に | 網羅性 |
| T2 | 概要を **ちょうど3行**で `abstract.md` に | 制約遵守＋修正（C→A→再D） |
| T3 | `pytest` 実行、通過数を `result.md` に | 実行＋事実照合 |
| T4 | `tests/` のテスト関数総数を `count.md` に | 正確性 |

## 先に規定した失敗点

- **収束しない** → 各層のハード上限（呼び出し側規定）
- **Reflect 誤判定**（誤合格/誤不合格）→ L2 の生命線。構造化出力＋プロンプト設計を難タスクで詰める
- **再帰の底** → 人間

## 進め方（確定ルール準拠）

- 1層=1ファイル `mu/l2.py` ／ `l2_chat.py` を用意 ／ 内側から一層ずつ完成
- TDD・テスト通過で評価 ／ 実接続（難タスク T1〜T4）で Reflect を詰める
- 破壊系タスク前に復元ポイント確保済み（commit `0733abf` push 済み）

## 検証と修正（2026-07-03〜04 / スプリント進行中）

- L2 実装: `mu/l2.py`（`Agent`, step/run）・`l2_chat.py`（step 駆動表示）・`tests/test_l2.py`（8件）。全体 **39 green**（live 除く）。
- 実タスク T2（3行制約）で Reflect を実機検証。得た知見:
  - **F3 採用**: 弱いモデルへの対症療法を続けず、参照モデルを **qwen3.5:9b / gemma4:12b**（ともに tools 対応・特定モデルへの過剰最適化回避で2つ）に。4b のパス幻覚は 9b で解消。
  - **Reflect 修正**: ① フィードバックを **system ロール**で戻す（`_transcript` から除外＝**汚染防止**）② **think=False** ＋ JSON 頑健抽出で構造化出力を安定化（"unparseable" 解消）。
  - **最大の学び**: 「3行」が曖昧で収束せず → **ゴールの成功条件は checkable にする**（skill 化: `goal-success-criteria-must-be-checkable`）。Reflect ではなくゴール定義が原因だった。
## 検証完了（2026-07-04）

T2〜T4 と T1 を実機で通した。**checkable なゴール＋公正な Reflect** が鍵:

- **T2**（制約＋修正）qwen3.5:9b ✓ ／ **T4**（正確性）qwen ✗（モデル固有のパス破損）→ **gemma4:12b ✓** ／ **T1**（網羅性）gemma4:12b ✓ ／ **T3**（pytest 実行照合）gemma4:12b ✓。
- **Reflect 追加修正**: ① フィードバックを **user ロール＋目印**で戻す（steering 復活＋汚染防止を両立）② **「厳格だが公正」**（ゴールに明記の制約だけを判定し、大小文字・末尾空白等を勝手に足さない）③ `think=False`＋JSON 頑健抽出、unparseable 時の next を中立化。
- **学び**: **ゴールの成功条件は checkable に**（skill 化: `goal-success-criteria-must-be-checkable`）／ 参照2モデルにした狙いが的中（T4 の qwen 失敗を「mu のバグ」と誤診せずに済んだ）。
- 残（軽微・任意）: `l2_chat` の最終応答表示が tool 終端で空になる件。L3／明示 Plan は次段。

> **L2 完成**（概念＋実装＋テスト＋実タスク検証, 2026-07-04）。テスト全体 **42 green**。

> **L3 の方向（振り返りで確定 2026-07-04）**: **大域的（明示）Plan は L3 の仕事**。L2 は局所・暗黙の計画（Reflect の next 再投入）に留める。L3 は同じ再帰同一構造で L2 を管理・打ち切る外皮でもある。

