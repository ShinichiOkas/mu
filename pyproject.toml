[project]
name = "mu"
version = "0.0.1"
description = "きわめてミニマルな汎用エージェント"
requires-python = ">=3.11"
dependencies = ["ollama>=0.6"]

[project.optional-dependencies]
dev = ["pytest>=8"]

[tool.pytest.ini_options]
pythonpath = ["."]
markers = [
  "live: 実 Ollama への接続を要するテスト（サーバ未起動ならスキップ）",
]
