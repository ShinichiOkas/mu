# 走行記録 2026-08-08-019p4 — deadstock 再走（019 Phase 4: 契約の system 搬送後）

コマンド: `probe_hard.py deadstock gemma4:31b-cloud runs/2026-08-08-019p4/deadstock qwen3.5:9b`
経過: 1771s（締切 1500s＋タスク境界超過 4.5分）・自然終了 exit 0・violations なし

## 結果: achieved=false（uncertain）。ただし3走の中で最も健全な走行

| 観点 | 結果 |
|---|---|
| **報告書の正しさ** | **初めて正解**（P007・P010 のみ＝真の死に筋。p008 の小文字罠も正しく処理。捏造・混入ゼロ） |
| **判定書の形式** | **初めて正規**（ITEM 1〜4 の番号付き・PASS/PASS/PASS/FAIL・GAP 付き）——**契約の system 搬送が効いた** |
| **未検査の可視化（015）** | **初の実発火**。PdM が基準#4（死に筋の具体的記載）の run を正しく空にし、「機械的に検査されていない受入基準が 1 件ある」が表示された |
| 締切超過 | 33%→18% に縮小 |
| 完遂 | 未達。QA タスクが L3 の周回内に done にならず、rerun 判断→3周目頭で締切 escalate |

## 何が残ったか

1. **L3 が QA の unit に自作の検査コマンドを発明し、その検査自体が壊れている**。
   `expect: "'ITEM 1:' and no code blocks"`（そんな出力は無い）、`Write-Content`（存在しない
   コマンドレット）、ParserError になるワンライナー——**正しい判定書が3回、壊れた自作検査で
   落とされた**。判定書には `read_verdict` というコード側の読み手が既にある。unit の検査を
   LLM に発明させる必要がない。対策候補: plan_lint で QA 成果物の unit の check を
   **コード供給の正準検査**（`Get-Content verdict.md` → expect `ITEM 1:`）に差し替える
2. **「判定を直しに行く」圧力は1回だけ残存**（L2 が ITEM 4 FAIL を理由に passed=False、
   ログ1049行）。019 実走の反復圧力からは大幅減。規範は system で届いても確率的にしか
   効かない——決定論の防壁（集約がコード）は3走とも守られたので、実害は周回の浪費
3. **QA の ITEM 4 FAIL は事実誤認**（P007/P010 で列挙は完全なのに「不足」と判定）。
   正直だが誤った FAIL。列挙の網羅性の判定は全量の再計算を要する——judge を配ったが
   QA は使わなかった。ただし構造上、誤 FAIL は「未達→再実行」に倒れるだけで
   偽の達成にはならない（安全側）
4. verdict.md は完成していたのに、QA タスクが done でないため read_verdict が
   呼ばれず「no verdict」になった（タスク失敗時は判定書を信用しない設計。今回のように
   「壊れた自作検査だけが失敗理由」の場合は損）

## 3走の推移（同一課題・同一モデル）

| 走 | 報告書 | 判定書 | 結果 |
|---|---|---|---|
| 018 | P003 捏造（マーカー強制） | 独自テーブル形式 | uncertain（項目なし） |
| 019 | P004 混入（素のバグ） | 商品コード単位 ITEM | uncertain（番号不一致） |
| 019p4 | **正解** | **正規番号付き・正直な FAIL** | uncertain（QA タスク未完了） |

偽の達成は3走とも**ゼロ**。成果物と判定の質は単調に改善。残る血栓は
「L3 の自作検査」1点に絞られた。
