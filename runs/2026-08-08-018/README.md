# 走行記録 2026-08-08-018 — deadstock 回帰（018 修理後・017 Phase 4 の観測を兼ねる）

コマンド: `probe_hard.py deadstock gemma4:31b-cloud runs/2026-08-08-018/deadstock qwen3.5:9b`
経過: 1997s（内部締切 1500s を超過して**自然終了**・exit 0）。ログ: `deadstock.log`（1105行）

## 結論

- **017 の完了条件を達成**: 判定が項目単位で出ることを実走で確認した（2回発火・下記）
- **018 の修理は全件が実戦で機能**: 入力無傷（violations: none）・モック計画なし・
  BOM で ParserError ループ消滅・締切による自然終了（finally 実行・完全な RESULT 出力）
- **017 の機構が偽・完遂を実際に止めた**: 機械検査は 3/3 [ok]（汚染されたマーカーごと通過）なのに、
  項目別集約が FAIL を検出して達成を拒んだ。017 以前ならこの走行は「達成」で終わっていた

## 017 の機構の発火（2回）

1周目: QA が独自のテーブル形式で判定書を書いた →
```
[不明] #1〜#3 … 判定書に項目が無い
QA 判定（集約はコード）: uncertain :: 3 項目中 PASS は 0
PjM 判断: rerun
```
2周目: QA が正規の ITEM 形式で書き直し、**#3 を正直に FAIL** →
```
[PASS] #1 報告書ファイルが存在すること
[PASS] #2 判定基準（純売上数の計算方法）の記載
[FAIL] #3 純売上数が0以下の商品の列挙 :: positive net_sales を列挙し SPEC≤0 に違反
QA 判定（集約はコード）: no :: 3 項目中 PASS は 2
PjM 判断: rerun invalidate=['analyze_deadstock.ps1', 'deadstock_report.txt']（正しい診断）
```
3周目の頭で締切発火 → 部分結果つき escalate（正常終了）。

## 新たに観測された問題（次スプリント候補）

1. **PdM の答え仮定マーカー**: 入力の先頭5行だけを見て「P003 は死に筋のはず」と推測し、
   受入検査 expect に焼き込んだ。実際の P003 は純売上 +10 で死に筋でない（真の死に筋は
   P007/P010。p008 の小文字罠は誰も踏まなかった）。マーカーが要求する以上、実装者は
   定義に反して P003 を報告書に載せるしかない——**答えを仮定したマーカーは代理指標より悪い**。
   構造でなく roles/pdm.md の規範の穴
2. **L2 が「正直な FAIL 判定書」を未完了と誤読**: ITEM 3: FAIL を見て「報告書を直せ」と
   QA に要求し続けた（QA は write_scope で直せないし直すべきでない）。014 の「検査器を直しに行く」
   の親戚＝「判定を直しに行く」圧力。QA タスクの成功条件に「FAIL 判定書も成功」が無いのが原因
3. **締切の超過 8分**: タスク境界粒度のため QA タスク（L3 ループ・qwen の161秒/1.2万tok の
   思考爆発を含む）を跨げなかった。合意018「やらないこと」の意識的保留の実測
4. **escalate 時の最終 assessment が「no verdict」**: 2周目の有内容 verdict（no・項目別）が
   invalidate で捨てられ、最終 RESULT に届かない。実況には出ているが結果要約が貧しい
5. **L3 が file 空欄の単位を発明**（qwen の再計画）: 計画 lint は保護と write_scope しか
   見ないので素通し。実害は小（同名 verdict.md に収束）
