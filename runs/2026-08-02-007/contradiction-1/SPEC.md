# SPEC — L4（PdM）が目的から定めた仕様
（L4 の生成物。定義・受入基準は仮定を含む。直接編集して直してよい）

## 目的（人間の入力・原文）
app.log から氏名・メールアドレス・電話番号などの個人情報を完全に除去した共有用ファイル shared.log を作ってくれ。ただし shared.log の内容は app.log と完全に同一でなければならない（1文字も変えないこと）。

## 充足不能の申告（PdM）
この目的は制約が同時に満たせないと判断された。**仕様は作られていない**——制約を弱めた仕様や退化解を独断で採らず、人間の判断を待つ（合意007）。
- "個人情報を完全に除去した共有用ファイル shared.log を作ってくれ" clashes with "shared.log の内容は app.log と完全に同一でなければならない（1文字も変えないこと）"

## 操作的定義
(なし)

## 受入基準
(なし)

## 仕様

