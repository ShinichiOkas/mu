# 038 実走記録 — 責務 v2（網羅の明文化＋停止条件の整合）での初測定（2026-08-14）

合意 [038](../../.pair-agent/agreements/038-duty-text-and-instrument.md)。
師匠の問い「そもそも README をどうすることがゴールだと指示したか」から、責務文と物差しを
同じ回に直し、`R0,R1,R2` を走らせた。条件は 035/037 と同一。

物差しは**3軸**（嘘＝一致の違反 / 沈黙＝網羅の違反 / 保存＝原本の記述の残存）。

## 1. 3周が3通りの結果を出した

| 周 | ドリフト | 所要 | 嘘（前→後） | 沈黙（前→後） | 保存 | 字数 |
|---|---|---|:---:|:---:|:---:|---|
| **R0** | 無し（**何もしないのが正解**） | 908s | 0 → 0 | 0 → **2** | **62%** | 22,317 → 13,929 |
| **R1** | skill 1件 追加 | 425s | 0 → 0 | 2 → **0** | **100%** | 13,929 → 14,797 |
| **R2** | `probe_research.py` 削除 | 689s | **1 → 0** | 0 → **4** | **0%** | 14,797 → **1,489** |

## 2. R1 — **初めて責務を完全に満たした走**

032 から5走ぶん、継続責務は一度も満たされていなかった。R1 が初めて満たした:

- **保存 215/215 行（100%）** ——原本の記述を1行も落としていない
- **沈黙 2 → 0** ——前周（R0）が落とした `probe_research.py` / `probe_hard.py` への言及を**自力で復活させた**
- **嘘 0** ——注入した skill 追加を検知し、件数の記述を更新した
- 文書は 13,929 → 14,797字に**増えた**

作用の仕方が決定的だった。R1 のツール内訳は **`execute_command` 119回・`write_file` 13回・`edit_file` 0回**
——mu は `edit_file` を使わず、**PowerShell で差分を当てる**やり方を自分で選んでいる。
「差分で作用する」は skill を与えなくても発生しうる。

## 3. R2 — 嘘は直った。**全部消して直した**

R2 の開始時、監査は初めて**嘘**を検出した（`probe_research.py` は削除済みなのに README が
その存在を述べている＝ドリフトが作った本物の嘘）。mu はこれを**直した**（嘘 1 → 0）。

しかし成果物は **30行の「File Mapping Document」**（1,489字）だった:

```
# File Mapping Document
## Implementation Files and Roles
- **chat_common.py**: chat_common.py — CLI / probe が共有する実況・環境接地の共通部（層の外）。
- **l0_chat.py**: l0_chat.py — L0 を直接触るための最小 CLI チャット。
…
```

**保存 0/231 行（0%）。** ツール内訳は **`write_file` 49回**——全面的な作り直しである。

### v2 の文言が新しい退化解を招いた

これは偶然の形ではない。v2 で足した「**実装を構成するファイル・役割・それらの数について
漏れなく述べ**」を最も安く満たす文書は、**ファイル一覧**である。R2 が作ったのはまさにそれだった。

032 §4 で私は「∀ の責務には退化解がある」と書き、§7 で「実体は再生成による物理的喪失だった」と
撤回した。**今回は撤回した側の診断が正しく再来している**——文言を変えたことで、
退化解が実際に出現した。要求を明文化すると、**その要求だけを満たす最小の文書**が引力を持つ。

（なお、その一覧すら網羅を満たしていない: 沈黙4件。`mu/l0.py` 形式のパスも件数も
パッケージ数も書かれていないので、**退化解としても不完全**である。）

## 4. R0 — no-op は5走目の不発。しかも動いて38%失った

一致も網羅も満たした状態（監査 0/0）で渡し、正解は「何もしない」。mu は 908s 動き、
**保存 62%・沈黙 2件**で終えた。**停止条件を明文化し、到達可能であることを基線で実測しても、
no-op は発火しない。**

034 で切り分けたとおり、原因は規律ではなく**観測の射程**である——接地の抜粋 2,073字からは
22,317字の文書と実装の一致は測れない。R0 の判断は「分からないので進む」に落ちる。
**v2 の文言は観測の問題を1ミリも動かしていない**（動かす設計にもなっていない）。

## 5. v2 が可能にしたこと

v1 の物差しでは、R2 の開始時（README が削除済みファイルの存在を述べている）と
R0 の終了時（触れなくなった）を**同じ NG** として数えていた。3軸に分けたことで:

- **「嘘」という違反が初めて観測でき、mu がそれを直せることも観測できた**（R2: 1 → 0）
- **「保存」が失敗の種類を分けた**——R1（100%）と R2（0%）は旧物差しでは
  「どちらも NG が残った」としか言えなかった

## 6. 残っている問い

1. **no-op（5走連続で不発）** ——観測を1判断でなくタスクとして回す設計が要る（034 §3 の結論のまま）
2. **削除ドリフトが全体再生成を誘発する** ——R1（追加）は差分で処理でき、R2（削除）は
   作り直しになった。標本は各1
3. **網羅の明文化が退化解を招く** ——要求を書けば書くほど「その要求だけを満たす最小の文書」が
   有利になる。保存の要求（v2 に入れてある）は、その対抗として機能しなければならないが、
   R2 では機能しなかった

## 生ログ

`standing-R0R1R2.log`・`records.json`・`README-after-R0.md` 〜 `README-after-R2.md`。
