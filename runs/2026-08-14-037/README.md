# 037 実走記録 — 穴を塞いだ結果（フェーズ E・5走）2026-08-14

合意 [037](../../.pair-agent/agreements/037-close-the-holes.md) の実証。
条件は 035/036 と同一（`gemma4:31b-cloud`・並列1・`MU_ROLES_DIR=auto`・物差し無変更）。

## 1. 結果の要約 — **2つ塞がり、1つは開いたまま**

| 穴 | 走 | 前 | 後 | 判定 |
|---|---|---|---|:---:|
| **A** 原本が書き手に届かない | standing R1/R2 | 5.5%（2,638字） | **21%（10,018字）** | **半分** |
| **B** ゲートが宣言放棄を許す | （発火せず） | — | — | **未実証** |
| **C** 絶対パス・契約の改変 | repro | 6/7・4/7（偽・完遂2件） | **7/7 PASS** | **塞がった** |
| **D** 基準の後付け | compat | 12/13 ×2 | **13/13 PASS** | **塞がった** |
| （回帰） | maintain | 16/16 / 8/16 | 8/16 FAIL | **既知のばらつき** |

## 2. A — 機構は設計どおり動いた。**それでも破壊は止まらなかった**

implementer の tray に原本が入ったことは実物で確認できた:

```
[L4] [<] 原本を写した: README.md（更新対象は入力でもある）
[tool]   -> file alignment_plan.md (3337 bytes) file README.md (47716 bytes) file SPEC.md (3109 bytes)
```

**`needs` に README.md は宣言されていない。** それでも原本は手元にある——035 の穴は塞がった。

にもかかわらず、README は 47,715 → **10,018字**（R1）→ 9,218字（R2）になった。
ログを読むと理由は明快である:

| | 回数 |
|---|---|
| `read_file` | 81 |
| `write_file` | 14 |
| **`edit_file`** | **0** |

**原本を81回読んで、書き直して、尽きた。** これは 032 E-1（接地だけ直した走）の
**10,436字とほぼ同じ地点**である。つまり A が回復させたのは「原本にアクセスできる」ことまでで、
**「作用が差分である」ことは別の穴**だった。

### そして、その穴の対処は既に測ってある（私のスコープ設計の漏れ）

032 フェーズ E-2 で、`edit-do-not-regenerate`（既存ファイルは `edit_file` で差分を当てる）を
与えた走は **`edit_file` 3回・29,668字＝破壊が止まった**。この skill は
[`runs/2026-08-13-032/loop-skills/`](../2026-08-13-032/loop-skills/) に置いたまま、
**リポジトリの `skills/` に取り込まれていない**。

037 の C/D を「036 で観測した失敗だけ」に絞ったとき、**032 で測定済みの対処を持ち込むことを
落とした**。塞ぐべき穴の一覧に入れていなかったのは私の設計ミスである。

## 3. C・D — 狙った失敗はどちらも消えた

**repro（7/7 PASS・144s）** — 036 で偽・完遂を2件出した題材:

| 036 で起きたこと | 037 |
|---|---|
| `CUSTOMERS = "S:/tmp/mu-036/repro-r1/customers.csv"` | `CUSTOMERS = "customers.csv"` |
| 列名を `total` → `total_amount` に改名 | `REPORT_COLUMNS = (..., "total", "orders")` のまま |

**見せていないデータでも正しく、顧客表に無い注文の除外も保たれている。** 隠し検査7項目すべて ok。

**compat（13/13 PASS・98s）** — 署名は頼んだとおり `render(rows, title, min_width=None, thousands=False)`。
呼び出し元3本の出力は1バイトも変わらず、新オプションも仕様どおり。
036 で自滅した「変更後に golden を作る」は再現しなかった。

いずれも**1走**なので、skill が効いたと断定はしない。ただし**036 で2走とも出た具体の失敗が、
037 では出ていない**。

## 4. maintain — 既知のばらつきの範囲（3走中2走がこの形）

FAIL 8/16・951s・`rerun` 3回で予算切れ。理由は 036 r1 と同一である:

> The implementer failed to actually write the required functions into taskstore.py;
> the file content shows only the module docstring.

**既存の公開 API は8項目すべて ok**（原本は無傷）で、壊れたのではなく**足せなかった**。
同じ題材の3走は 91s で 16/16 PASS・636s で escalate・951s で escalate ——
**7〜10倍のばらつき**があり、n=1 では何も帰属できない（035 の教訓）。

なお、この失敗も形は「作り直しに失敗して docstring だけになった」であり、§2 の
**作用が差分でない**問題と同じ家族である可能性が高い。

## 5. B — 発火しなかった（未実証）

5走とも needs ゲートの未充足が起きなかったため、候補提示は実走で観測できていない。
ユニットテストでは固定してある（`tests/test_l4.py` / `tests/test_process.py`）。

## 6. 次にやるべきこと（この走が絞ったもの）

1. **`edit-do-not-regenerate` を `skills/` に取り込む**——032 で測定済み・未採用。
   A（原本が手元にある）と組み合わさって初めて「観測 → 差分 → 差分だけ作用」が揃う
2. その上で standing R1/R2 を再走し、**21% → どこまで戻るか**を同じ物差しで測る
3. maintain のばらつき（3走中2走が失敗）は独立した問題として残っている

## 生ログ

`standing-R1R2.log`（2周）・`repro.log` / `compat.log` / `maintain.log`・`smoke-bugfix.log`。
