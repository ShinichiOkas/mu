# 031 実走記録 — 並列スケジューラ（2026-08-12）

合意 [031](../../.pair-agent/agreements/031-parallel-scheduler.md) フェーズ C の検証。
師匠の受け入れの枠:「おそらく速くならないとは思っているが、**副作用で悪化しないことの確認**が
必要」。確かめたのは3つ——**並列が実走で正しく発火すること**・**悪化しないこと**・
**速くなるかを数字で言えること**。

## 1. 結果の一覧

| 走 | 条件 | achieved | 所要 |
|---|---|---|---|
| width p1 | gemma4:12b・逐次 | true | 572s（rounds=2——PjM の再実行が1周入った走） |
| width p1b | gemma4:12b・逐次 | true | **127s**（rounds=1） |
| width p2 | gemma4:12b・**並列2** | true | **177s**（rounds=1・**並列発火**） |
| bugfix 31b | gemma4:31b-cloud・**並列2**（一本道） | true | **103s** |

全走で偽・完遂なし・protection violations none・needs 未充足 0・発行拒否 0。

## 2. 主デリバラブル: 悪化しないことの確認 — **成立**

- **一本道×並列2（bugfix 31b-cloud）: 103s** — 030 の同条件逐次 198s・029 の最良 105s と
  同水準。幅の無いプロセスでは並列スケジューラ経由でも逐次と同じに走り、
  オーバーヘッドは観測されない
- **幅×並列2（width p2）: 177s** — 逐次サンプル（127s / 572s）のばらつき範囲内。悪化なし
- 正しさの面でも: tray 拒否 0・needs 未充足 0・発行拒否 0・QA バリア順守（両タスク完了後に QA）

## 3. 並列は実走で発火する（機構の実証）

width p2 のログ: `TASK START poem_a.md` → poem_a 実行中に `TASK START poem_b.md` →
poem_b 完了 → poem_a 完了 → QA。**独立2タスクの同時実行・完了順の逆転・QA バリア**が
実機で観測された。PjM（12b）は幅のある仕様に対して自然に幅のあるプロセス
（独立2タスク＋QA）を編んだ——「幅の促し」の規範なしで。

## 4. 速くなるか — ローカル 12b ではノイズに埋もれる（師匠の予想どおり）

12b の走間ばらつき（同一条件の逐次で 127s〜572s、4.5倍）が、並列の理論利得
（このプロセスでは高々 taskの重なり分）より一桁大きい。**この環境・この題材では
並列の速度利得は観測できない**。フェーズ0の L0 プローブでは並列自体は効いている
（直列2本 1.2s → 並列 0.7s）ので、利得が現れる条件は「タスクが長く・幅が広く・
モデル実行が併走に耐える」場合（クラウドモデル前提の本線と整合）。

## 5. 実走が掘り当てた欠陥2件（どちらも機構で修理）

1. **l4_chat が SPEC.md を書いていなかった**（初回 width p1 = 1752s の主因）。goal が
   「参照できるファイル: SPEC.md」と実在しないファイルを名指しし、QA が探して空転。
   L5 経由では Director が書くため 030 のスモークでは露見しなかった。修理: l4_chat が
   cwd に SPEC.md を実体化（環境接地は caller の責務）。1752s → 572s
2. **読み取り専用の写しに殺される**（初回 bugfix 31b×並列 = 例外死）。実行者が skill
   「入力ファイルは読み取り専用」に従い tray の写しに +R を立て、rerun 周の copy_in が
   PermissionError で run ごと死んだ——**並列固有ではなく 030 の潜在欠陥**（逐次の rerun
   でも起きる）。修理: 写し・残骸・自成果物の前版は属性に関わらず消してから作り直す
